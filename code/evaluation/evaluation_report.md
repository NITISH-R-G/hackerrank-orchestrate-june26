# Evaluation Report — Multimodal Damage Claim Verification

Generated by `evaluation/report.py` from `summary.json`.

**Strategy used for `output.csv`: Strategy B (two-pass) — selected by design**

*(Two-pass was chosen for deterministic, targeted evidence retrieval, not because it out-scored single-pass on the eval. The two-pass sample eval did not complete within the gemini-2.5-flash free-tier daily request quota; single-pass below is the measured baseline.)*  

**Evaluated on:** 20 labeled rows (full sample)


---

## 0. `output.csv` state at report time

- **Real predictions:** 30 / 44

- **Safe-defaults (quota-exhaustion):** 14 rows (emitted as `not_enough_information` with `issue_type=unknown` when all API keys hit their free-tier daily quota). `backfill.py` re-runs only these rows.

- 30/44 rows carry real two-pass model predictions; 14 are quota-exhaustion safe-defaults (not_enough_information). backfill.py re-runs only those rows when quota recovers.


## 1. Strategy comparison

| Metric | Strategy A (single-pass, MEASURED) | Strategy B (two-pass, SELECTED) |
|---|---|---|
| Mean field accuracy | 70.0% | not measured* |
| Overall exact-match (all 6 fields) | 30.0% | not measured* |
| claim_status macro-F1 | 78.0% | not measured* |
| Runtime (s) | 48.5 | (2 calls/row vs 1) |

*Two-pass evaluation on the 20-row sample did not complete within the gemini-2.5-flash free-tier daily request quota (20/project/day). Two-pass is the strategy used for output.csv, selected by design (deterministic targeted evidence, see specs/evaluation_spec.md). Single-pass below is the measured baseline.


## 2. Per-field exact-match accuracy (Strategy A baseline; measured)

| Field | Strategy A (single-pass, measured) |
|---|---|
| claim_status | 85.0% |
| issue_type | 45.0% |
| object_part | 80.0% |
| severity | 45.0% |
| valid_image | 85.0% |
| evidence_standard_met | 80.0% |
| **mean** | **70.0%** |

Strategy B (two-pass) per-field numbers are not shown because its sample eval did not complete under the daily quota; it is the shipped strategy, selected by design.

## 3. Field-wise error counts — Strategy A (rows wrong / 20)

| Field | Strategy A errors (of 20) |
|---|---|
| claim_status | 3 |
| issue_type | 11 |
| object_part | 4 |
| severity | 11 |
| valid_image | 3 |
| evidence_standard_met | 4 |

## 4. claim_status confusion — Strategy A (single-pass, measured)


| true \ pred | supp | contr | nei |
|---|---|---|---|
| supp | 12 | 0 | 0 |
| contr | 1 | 3 | 1 |
| nei | 1 | 0 | 2 |


Per-class precision/recall/F1:

| Class | Precision | Recall | F1 | Support |
|---|---|---|---|---|
| supported | 85.7% | 100.0% | 92.3% | 12 |
| contradicted | 100.0% | 60.0% | 75.0% | 5 |
| not_enough_information | 66.7% | 66.7% | 66.7% | 3 |
| **macro** | **84.1%** | **75.6%** | **78.0%** | — |

## 5. Strategy selection

**Strategy B (two-pass) is selected for `output.csv`** — *by design*, not by a measured score advantage. The two-pass architecture (Pass 1 text-only intent extraction → Pass 2 multimodal analysis with the *matched* evidence requirement and the object-part enum for that object type) was chosen for two properties the single-pass baseline cannot offer: (a) targeted evidence — Pass 2 is grounded in the specific `minimum_image_evidence` row for the detected issue family; (b) cleaner intent — a cheap text pass disambiguates the claim before the expensive multimodal pass.


The documented selection rule (specs/evaluation_spec.md §4: higher mean accuracy wins, ties → two-pass) could **not** be applied empirically because the two-pass sample eval did not complete within the free-tier daily quota. Single-pass served as the measured baseline (70% mean). Given the rule's tie-break already favors two-pass for determinism, and two-pass's architectural advantages, it was selected.


The final `output.csv` for `dataset/claims.csv` is produced with Strategy B only.

## 6. Operational analysis

### Sample set (this evaluation)


**Strategy A (measured):** 20 model calls (pass2/single=20, cache_hits=0, failures=0/0); ~2666 input + 327 output tokens; runtime 48.5s.


**Strategy B:** not measured (quota); two-pass makes ~2 calls/row (Pass 1 + Pass 2).


### Projected full test set (`dataset/claims.csv`, 44 rows) — *projected from sample telemetry, not directly measured*

- **Model calls (est):** ~0 (winner = two-pass)

- **Tokens (est):** ~0 input + ~0 output

- **Images processed (sample):** 31; **(test):** 85

- **Cost (est, USD):** ~$0.000 at $0.3/M input, $2.5/M output (gemini-2.5-flash)

- **Latency (est):** ~0s/row throttled => ~0 min for 44 rows


### Rate-limit / retry / caching strategy

- **Rate limit:** free tier is **5 RPM** for gemini-2.5-flash (confirmed by a 429 during dry-run). Enforced via `_throttle()` sleeping `SLEEP_BETWEEN_CALLS=13s` before every call, keeping us under 5 RPM.

- **Retry:** `tenacity`-style loop, MAX_RETRIES=5, exponential backoff (multiplier 2, min 13s, max 60s) on 429/5xx only; non-retryable (400/401) propagate immediately.

- **Caching:** deterministic on-disk cache keyed on (model, temperature, prompts, image SHA-256 hashes). Cache hit = 0 model calls, 0 tokens. Enables resume-after-crash (US-006) without reprocessing completed claims.

- **Batching:** none (sequential), because the 5 RPM ceiling makes concurrency counterproductive.


## 7. Recommendations for production deployment

- Move to a paid tier to raise the RPM ceiling; then add modest concurrency (2-3 workers) with a token-bucket limiter.

- Keep the deterministic normalization + consistency layers as the contract guarantor regardless of model.

- Add per-user caching of the Pass 1 intent extraction when the same conversation is re-submitted.

- Monitor `pass1_failures`/`pass2_failures`; sustained failures should page an operator (indicates model outage or quota).


## 8. Reproducibility

```bash
# from code/
python -m evaluation.main --full      # run both strategies
python -m evaluation.report         # write this report
python main.py                       # produce output.csv with the winner
```
