[pytest]
testpaths = tests
markers =
    live: makes real Gemini API calls (needs GEMINI_API_KEY)
addopts = -m "not live"
filterwarnings =
    # Third-party: google-genai uses _UnionGenericAlias (slated for removal in Py3.17)
    ignore::DeprecationWarning:google.genai.*
